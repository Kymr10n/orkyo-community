name: Security Refresh

on:
  schedule:
    - cron: '17 2 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  refresh-frontend:
    permissions:
      contents: read
      packages: write
      issues: write
    uses: Kymr10n/orkyo-foundation/.github/workflows/reusable-security-refresh.yml@52096ef653daba7eb45d360d4901013d06a2a3cf
    with:
      image-name: orkyo-community-frontend
      needs-ghcr-secret: false
    secrets: inherit
